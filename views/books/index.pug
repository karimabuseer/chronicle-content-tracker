extends ../layout

block content
  a(href="/books/new") Add Book
  h1= title
  p Welcome to #{title}

  list(class='booklist')
    ul(class='book')
      each book in bookList
        div author
        div(class='author') #{book.user_id}
        div title
        div(class='title') #{book.book_id}
