extends ../layout

block content
  h1= title
  p #{title}
  form(class='sign-up-form form', action='/users', method='POST')
      div.label 
      div.username
        input(type="text", name="username", placeholder="username")
      div.email
        input(type="email", name="email", placeholder="email")
      div.password
        input(type="password", name="password", placeholder="password")
      button(type="submit", value="Submit" id='sign-up-button' onClick='clearForm()') Sign Up
      p
        | Already a member? 
        break 
        a(href="/session/new") Log In Here